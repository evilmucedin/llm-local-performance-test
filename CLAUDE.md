# Claude guide

Claude Code and related Claude coding tools should use `AGENTS.md` as the canonical project instructions file.

Start with:

1. `README.md` for user-facing behavior.
2. `ARCHITECTURE.md` for module layout and data flow.
3. `AGENTS.md` for commands, validation, and PR checklist.

Keep this file short so agent instructions do not drift between tools.
